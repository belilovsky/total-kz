# total.kz — fail2ban filter for DDoS / high request rate
# Matches any request — fail2ban triggers when maxretry (100 req/10s) is exceeded

[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD|PUT|DELETE) /

ignoreregex = ^<HOST> .* "(GET) /health
