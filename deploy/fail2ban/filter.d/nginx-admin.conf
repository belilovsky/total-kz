# total.kz — fail2ban filter for admin login brute-force
# Matches failed login attempts to /admin/login

[Definition]
failregex = ^<HOST> .* "(GET|POST) /admin/login.*" (401|403)
            ^<HOST> .* "(POST) /admin/login.*" (302|200)

ignoreregex =
